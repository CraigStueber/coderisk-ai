# Example requirements.txt with deprecated packages
# This file demonstrates packages that are deprecated or unmaintained

# pycrypto is deprecated - should use pycryptodome instead
pycrypto==2.6.1

# oauth2 is largely unmaintained - should use oauthlib
oauth2>=1.9.0

# nose is officially deprecated - should use pytest
nose==1.3.7

# django-rest-swagger is deprecated - should use drf-spectacular
django-rest-swagger==2.2.0

# python-jose has maintenance concerns
python-jose==3.3.0

# Some good packages mixed in (should NOT be flagged)
requests==2.28.0
cryptography==38.0.0
